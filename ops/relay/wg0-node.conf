# WireGuard — the HOME NODE side (template). Keeps the tunnel up from behind NAT.
[Interface]
Address = 10.9.0.2/24
PrivateKey = <NODE_PRIVATE_KEY>

[Peer]
# the rented server
PublicKey = <VPS_PUBLIC_KEY>
Endpoint = <VPS_IP>:51820
AllowedIPs = 10.9.0.1/32
PersistentKeepalive = 25
